import pytest
from django.urls import reverse

# @pytest.mark.test_order_view
# def test_order_view_order_review(db, client):
#     url = reverse('order:order_review', args=[order.id])
#     response = client.post(url)